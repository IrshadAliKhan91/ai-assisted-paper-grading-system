# Public, build-time configuration for the React app.
# These values are bundled into the browser: never add provider API keys here.

REACT_APP_API_URL=http://localhost:8010/api
REACT_APP_API_USER=admin
REACT_APP_API_PASS=replace_with_the_same_value_as_backend_admin_password
